alias.url = ( "/static/" => "/opt/rpi-monitor-server/static/" )

$SERVER["socket"] == ":5000" {
    $HTTP["url"] !~ "^/static/" {
        proxy.server  = ( "" => ( ( "socket" => "/tmp/rpi_monitor.sock" ) ) )
    }
}
